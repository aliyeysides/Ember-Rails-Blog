<h1 class="center caps border-bottom blue">Writing</h1>

    {{#each}}
      <li class="post">
        <h2>{{#link-to 'post' this class="dark-gray article"}}{{title}}{{/link-to}}</h2>
        <div class="article-details mid-gray">
          <li class="post blue">{{date}}</li>
        </div>
      </li>
    {{/each}}
    </ul>

{{outlet}}
